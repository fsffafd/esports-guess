{
  "name": "esports-guess",
  "version": "1.0.0",
  "description": "Esports Match Guessing System",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "deploy:fn": "tcb fn deploy api",
    "deploy:hosting": "tcb hosting deploy ./public",
    "deploy": "npm run deploy:fn && npm run deploy:hosting"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "bcryptjs": "^2.4.3",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2"
  }
}
